export { run } from '@oclif/command'

// import * as Config from '@oclif/config'
//
// import { run as oclif_run } from '@oclif/command'
//
// export function run(argv?: string[], options?: Config.LoadOptions): PromiseLike<any> {
//   if (argv && argv.length >= 1 && argv[0] === 'git') {
//     return Promise.resolve(true)
//   } else {
//     return oclif_run(argv, options)
//   }
// }
